# Loaded only by `vite --mode fullstack` (root `pnpm dev`): read and write through
# the local API, which Vite proxies to wrangler dev. The API signs local requests
# in as its dev user, so no token is needed.
VITE_API_URL=/api
