# Loaded only by `vite --mode fullstack` (root `pnpm dev`). Production builds never read
# this file, so the token below is never bundled into the deployed app.
VITE_API_URL=/api
VITE_ADMIN_TOKEN=dev-token
